[project]
name = "gmc-feed-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi==0.141.1",
  "pydantic-settings==2.15.0",
  "httpx==0.28.1",
]

[dependency-groups]
dev = [
  "pytest==9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
package = true
