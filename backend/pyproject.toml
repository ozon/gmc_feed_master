[project]
name = "gmc-feed-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi==0.141.1",
  "pydantic-settings==2.15.0",
  "httpx==0.28.1",
  "sqlalchemy==2.0.43",
  "asyncpg==0.30.0",
  "argon2-cffi==25.1.0",
]

[dependency-groups]
dev = [
  "alembic==1.16.4",
  "pytest==8.4.2",
  "pytest-asyncio==1.1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
package = true

[tool.setuptools.packages.find]
include = ["app*"]
